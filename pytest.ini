[pytest]
# The cache plugin's write path fails with "Operation not permitted" in some
# sandboxed dev setups, leaving stray pytest-cache-files-* dirs behind on
# every run. We don't use --lf/--ff/--cache-clear, so just disable it.
addopts = -p no:cacheprovider
